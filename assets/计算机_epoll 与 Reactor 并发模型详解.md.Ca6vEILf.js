import{_ as E,H as c,o as r,c as d,aO as i,q as n,s as a,a as o,l as s,aU as p,j as e}from"./chunks/framework.vjUPZZJn.js";const f=JSON.parse('{"title":"epoll 与 Reactor 并发模型详解","description":"","frontmatter":{"date":"2026-09-15T14:00:00.000Z","title":"epoll 与 Reactor 并发模型详解","source":[["1-知识/算法与八股/epoll 与 Reactor 并发模型面试题"]],"tags":["网络","学习笔记","epoll","Reactor"],"categories":["计算机"],"permalink":"/pages/d351cc"},"headers":[],"relativePath":"计算机/epoll 与 Reactor 并发模型详解.md","filePath":"计算机/epoll 与 Reactor 并发模型详解.md","lastUpdated":1789451739000}'),A={name:"计算机/epoll 与 Reactor 并发模型详解.md"};function B(h,l,g,u,P,T){const t=c("Mermaid");return r(),d("div",null,[l[6]||(l[6]=i('<h1 id="epoll-与-reactor-并发模型详解" tabindex="-1">epoll 与 Reactor 并发模型详解 <a class="header-anchor" href="#epoll-与-reactor-并发模型详解" aria-label="Permalink to &quot;epoll 与 Reactor 并发模型详解&quot;">​</a></h1> <p>Linux 高性能服务器绕不开两个主题：IO 多路复用（select/poll/epoll）和基于它的并发模型（Reactor/Proactor）。本文以经典 C++ 网络编程学习项目 TinyWebServer 采用的「单 Reactor 多线程」为例，把这两块串联讲清：模型怎么分工、epoll 为什么快、ET/LT 怎么选、以及 EPOLLONESHOT、线程安全这些常被追问的细节。</p> <h2 id="单-reactor-多线程模型怎么运转" tabindex="-1">单 Reactor 多线程模型怎么运转 <a class="header-anchor" href="#单-reactor-多线程模型怎么运转" aria-label="Permalink to &quot;单 Reactor 多线程模型怎么运转&quot;">​</a></h2> <p>以 TinyWebServer 为例，整体结构是「主线程监听 IO 事件 + 线程池做业务处理」：</p> <ol><li>主线程用 epoll 监听套接字上的 IO 事件；</li> <li>收到新连接请求时，accept 建立连接套接字，创建对应的连接对象与定时器（超时未活动则关闭连接），并把 fd 加入 epoll 管理；</li> <li>收到已连接客户的读写请求时，延长该连接的定时器有效期，并按事件类型向线程池任务队列投递任务——EPOLLIN 事件投递 read 任务，EPOLLOUT 事件投递 write 任务；</li> <li>主线程同时维护一个小顶堆定时器，删除超时节点，实现应用层保活；</li> <li>线程池中的工作线程被唤醒后从任务队列取任务：read 任务读取数据（ET 模式）并做后续业务处理（如用状态机解析 HTTP 报文），write 任务把生成的响应分散写回 socket；</li> <li>任务结束前用 epoll_ctl 调整 fd 状态：决定是否关闭连接（Keep-Alive）、继续监听哪个方向的事件。</li></ol> ',10)),(r(),n(p,null,{default:a(()=>[s(t,{id:"mermaid-44",class:"mermaid",graph:"flowchart%20LR%0A%20%20%20%20subgraph%20M%5B%E4%B8%BB%E7%BA%BF%E7%A8%8B%5D%0A%20%20%20%20%20%20%20%20R%5BReactor%3A%20epoll_wait%5D%20--%3E%20D%7Bdispatch%7D%0A%20%20%20%20%20%20%20%20D%20--%20%E8%BF%9E%E6%8E%A5%E5%BB%BA%E7%AB%8B%20--%3E%20AC%5BAcceptor%3A%20accept%5D%0A%20%20%20%20%20%20%20%20D%20--%20%E8%AF%BB%E5%86%99%E4%BA%8B%E4%BB%B6%20--%3E%20H%5BHandler%3A%20read%20%2F%20send%5D%0A%20%20%20%20end%0A%20%20%20%20H%20--%20%E6%94%B6%E5%88%B0%E7%9A%84%E6%95%B0%E6%8D%AE%20--%3E%20P%0A%20%20%20%20subgraph%20W%5B%E5%AD%90%E7%BA%BF%E7%A8%8B%5D%0A%20%20%20%20%20%20%20%20P%5BProcessor%3A%20%E4%B8%9A%E5%8A%A1%E5%A4%84%E7%90%86%5D%0A%20%20%20%20end%0A%20%20%20%20P%20--%20%E5%A4%84%E7%90%86%E7%BB%93%E6%9E%9C%20--%3E%20H%0A"})]),fallback:a(()=>[...l[0]||(l[0]=[o(`
          Loading...
        `,-1)])]),_:1})),l[7]||(l[7]=i('<p><strong>Reactor 只负责事件分发</strong>：连接建立事件交给 Acceptor 处理（accept 获取连接并创建 Handler），读写事件交给该连接对应的 Handler；Handler 不做业务，把收到的数据交给子线程里的 Processor，处理完的结果再经 Handler 发回客户端。</p> <p>关于线程池大小的经验值：CPU 密集型任务线程数取核数（四核取 4，最多 +1 余量）；<strong>IO 密集型任务线程数一般多于核数</strong>——线程间竞争的不是 CPU 而是 IO，多于核数的线程能在部分线程等 IO 时让 CPU 继续干活。</p> <h2 id="主从-reactor-与-proactor" tabindex="-1">主从 Reactor 与 Proactor <a class="header-anchor" href="#主从-reactor-与-proactor" aria-label="Permalink to &quot;主从 Reactor 与 Proactor&quot;">​</a></h2> <h3 id="主从-reactor-模型" tabindex="-1">主从 Reactor 模型 <a class="header-anchor" href="#主从-reactor-模型" aria-label="Permalink to &quot;主从 Reactor 模型&quot;">​</a></h3> ',8)),(r(),n(p,null,{default:a(()=>[s(t,{id:"mermaid-57",class:"mermaid",graph:"flowchart%20LR%0A%20%20%20%20MR%5BMainReactor%3Cbr%3E%E7%9B%91%E5%90%AC%E8%BF%9E%E6%8E%A5%E5%BB%BA%E7%AB%8B%5D%20--%3E%20AC%5BAcceptor%3A%20accept%5D%0A%20%20%20%20AC%20--%20%E5%88%86%E9%85%8D%E6%96%B0%E8%BF%9E%E6%8E%A5%20--%3E%20SR1%5BSubReactor-1%3Cbr%3Eepoll%20%2B%20Handler%5D%0A%20%20%20%20AC%20--%20%E5%88%86%E9%85%8D%E6%96%B0%E8%BF%9E%E6%8E%A5%20--%3E%20SR2%5BSubReactor-2%3Cbr%3Eepoll%20%2B%20Handler%5D%0A"})]),fallback:a(()=>[...l[2]||(l[2]=[o(`
          Loading...
        `,-1)])]),_:1})),l[8]||(l[8]=e("p",null,"MainReactor 只监听连接建立事件，accept 后把新连接分配给某个子线程；子线程的 SubReactor 把连接加入自己的 epoll 继续监听并处理后续事件。",-1)),l[9]||(l[9]=o()),l[10]||(l[10]=e("p",null,"多 Reactor 多线程的方案虽然看起来复杂，但实际实现往往比单 Reactor 多线程更简单：",-1)),l[11]||(l[11]=o()),l[12]||(l[12]=e("ul",null,[e("li",null,"主线程和子线程分工明确：主线程只接收新连接，子线程完成后续处理；"),o(),e("li",null,"交互简单：主线程只需要把新连接传给子线程，子线程无须返回数据，处理结果直接发给客户端。")],-1)),l[13]||(l[13]=o()),l[14]||(l[14]=e("h3",{id:"proactor-模型",tabindex:"-1"},[o("Proactor 模型 "),e("a",{class:"header-anchor",href:"#proactor-模型","aria-label":'Permalink to "Proactor 模型"'},"​")],-1)),l[15]||(l[15]=o()),(r(),n(p,null,{default:a(()=>[s(t,{id:"mermaid-79",class:"mermaid",graph:"flowchart%20LR%0A%20%20%20%20PI%5BProactor%20Initiator%5D%20--%20%E5%88%9B%E5%BB%BA%E5%B9%B6%E6%B3%A8%E5%86%8C%20--%3E%20PR%5BProactor%5D%0A%20%20%20%20PI%20--%20%E5%88%9B%E5%BB%BA%E5%B9%B6%E6%B3%A8%E5%86%8C%20--%3E%20HD%5BHandler%5D%0A%20%20%20%20PR%20%3C--%3E%20AOP%5BAsynchronous%20Operation%20Processor%3Cbr%3E%E5%86%85%E6%A0%B8%E5%AE%8C%E6%88%90%E5%BC%82%E6%AD%A5%20IO%5D%0A%20%20%20%20AOP%20--%20IO%20%E5%AE%8C%E6%88%90%E9%80%9A%E7%9F%A5%20--%3E%20PR%0A%20%20%20%20PR%20--%20%E6%8C%89%E4%BA%8B%E4%BB%B6%E7%B1%BB%E5%9E%8B%E5%9B%9E%E8%B0%83%20--%3E%20HD%0A"})]),fallback:a(()=>[...l[4]||(l[4]=[o(`
          Loading...
        `,-1)])]),_:1})),l[16]||(l[16]=i('<p>两者的本质区别：</p> <ul><li><strong>Reactor 是非阻塞同步网络模式，感知的是「就绪可读写」事件</strong>。每次感知到可读就绪后，需要应用进程主动调用 read 把数据从内核缓冲区搬进用户内存，这个过程是同步的，读完才能处理。</li> <li><strong>Proactor 是异步网络模式，感知的是「已完成」的读写事件</strong>。发起异步读写时传入数据缓冲区地址，内核自动完成读写并通知应用进程直接处理数据——读写全程由操作系统完成。</li></ul> <p>Linux 原生的异步 IO（AIO）并不成熟，实际项目里的「Proactor」多是「Reactor + 非阻塞 IO 模拟」出来的。</p> <h2 id="为什么用-epoll-与-select-poll-的区别" tabindex="-1">为什么用 epoll？与 select/poll 的区别 <a class="header-anchor" href="#为什么用-epoll-与-select-poll-的区别" aria-label="Permalink to &quot;为什么用 epoll？与 select/poll 的区别&quot;">​</a></h2> <h3 id="select-poll-的问题" tabindex="-1">select/poll 的问题 <a class="header-anchor" href="#select-poll-的问题" aria-label="Permalink to &quot;select/poll 的问题&quot;">​</a></h3> <p>select 把所有已连接的 Socket 放进一个<strong>文件描述符集合</strong>，调用 select 时把集合<strong>拷贝</strong>进内核，内核<strong>遍历</strong>检查每个 fd 有无事件，标记后把整个集合<strong>拷贝</strong>回用户态，用户态再<strong>遍历</strong>一遍找到就绪的 fd。于是：</p> <ol><li>每次调用要做 <strong>2 次遍历</strong>（内核态一次、用户态一次）和 <strong>2 次拷贝</strong>（进出内核）；</li> <li>select 用<strong>固定长度的 BitsMap</strong> 表示 fd 集合，个数受 <code>FD_SETSIZE</code> 限制（Linux 默认 1024，只能监听 0~1023）。</li></ol> <p>poll 改用 <strong>pollfd 结构数组</strong>，突破了个数限制（仍受系统 fd 上限约束），但没有本质区别：<strong>仍用线性结构存储 fd 集合</strong>，仍然要遍历、要拷贝，时间复杂度 O(n)。并发数上升后，每次调用的遍历与拷贝开销线性叠加，性能急剧下降。</p> <h3 id="epoll-的做法" tabindex="-1">epoll 的做法 <a class="header-anchor" href="#epoll-的做法" aria-label="Permalink to &quot;epoll 的做法&quot;">​</a></h3> <p>epoll 在内核里用<strong>红黑树跟踪</strong>进程所有待检测的 fd：epoll_ctl 把 socket 加入红黑树（增删改 O(log n)）。select/poll 内核不保存 fd 集合，每次都要整份传入；epoll 因为有这棵内核事件表，每次只需要操作单个 fd，省掉了大量拷贝和内存分配。</p> <p>epoll 用<strong>事件驱动</strong>：内核维护一个<strong>就绪链表</strong>，fd 上有事件时通过<strong>回调函数</strong>把它挂进就绪链表。epoll_wait 只返回有事件的 fd，不需要轮询扫描整个集合。</p> <p>补充两点：</p> <ul><li>select/poll 只能工作在<strong>相对低效的 LT 模式</strong>，epoll 同时支持 LT 和 ET；</li> <li>监听的 fd 数量少且都很活跃时，三者差别不大；<strong>fd 多而单位时间只有部分活跃时（典型的高并发服务器），epoll 优势明显</strong>。</li></ul> <h3 id="epoll-为什么选红黑树" tabindex="-1">epoll 为什么选红黑树 <a class="header-anchor" href="#epoll-为什么选红黑树" aria-label="Permalink to &quot;epoll 为什么选红黑树&quot;">​</a></h3> <p>epoll 需要在内核中长期维护一个存放所有待检测 fd 的数据结构，插入（EPOLL_CTL_ADD）、查找、删除操作频繁，需要一种三者效率都不错的结构，红黑树（增删查 O(log n)）是合适的选择。也因为有这棵内核事件表，epoll_wait 不必每次重新拷贝一遍所有 fd 进内核态。</p> <h2 id="边缘触发如何判断数据读完了" tabindex="-1">边缘触发如何判断数据读完了 <a class="header-anchor" href="#边缘触发如何判断数据读完了" aria-label="Permalink to &quot;边缘触发如何判断数据读完了&quot;">​</a></h2> <p>两种做法：</p> <ol><li>针对 TCP：调用 recv，若返回值小于设定的 recv buffer 大小，可认为本次接收完毕（对端可能还有后续数据，不严格）；</li> <li>TCP/UDP 通用（推荐）：先用 fcntl 把 socket 设为非阻塞，可读时循环 read/recv，直到返回 -1 且 errno 为 EAGAIN 或 EWOULDBLOCK——缓冲区已空，本次数据读完。</li></ol> <h2 id="客户端断开时-epoll-监听到什么" tabindex="-1">客户端断开时 epoll 监听到什么 <a class="header-anchor" href="#客户端断开时-epoll-监听到什么" aria-label="Permalink to &quot;客户端断开时 epoll 监听到什么&quot;">​</a></h2> <p>客户端正常断开（调用 close()）时，服务器端会触发 epoll 事件：</p> <ul><li><strong>早期内核</strong>：事件是 EPOLLIN（0x1），连接可读。上层在对端已 close() 的连接上读请求只能读到 EOF，靠 EOF 判断对端关闭；</li> <li><strong>后期内核</strong>增加了 <strong>EPOLLRDHUP</strong>（0x2000），代表对端断开连接或半关闭。此时触发的事件是 EPOLLIN | EPOLLRDHUP（0x2001），对端断开的处理可以<strong>在底层直接进行</strong>，不用再移交上层。</li></ul> <h2 id="epoll-的线程安全" tabindex="-1">epoll 的线程安全 <a class="header-anchor" href="#epoll-的线程安全" aria-label="Permalink to &quot;epoll 的线程安全&quot;">​</a></h2> <p>简要结论：<strong>epoll 通过锁保证线程安全</strong>。粒度最小的**自旋锁 ep-&gt;lock（spinlock）**保护就绪队列，**互斥锁 ep-&gt;mtx（mutex）**保护红黑树等核心数据结构：</p> <ul><li>epoll_ctl()：ep_insert() / ep_remove() 等操作自身数据结构前先持有 <strong>ep-&gt;mtx</strong>；</li> <li>epoll_wait()：持有<strong>自旋锁 ep-&gt;lock</strong> 保护就绪队列。</li></ul> <h2 id="水平触发-lt-与边缘触发-et" tabindex="-1">水平触发（LT）与边缘触发（ET） <a class="header-anchor" href="#水平触发-lt-与边缘触发-et" aria-label="Permalink to &quot;水平触发（LT）与边缘触发（ET）&quot;">​</a></h2> <h3 id="水平触发-lt" tabindex="-1">水平触发（LT） <a class="header-anchor" href="#水平触发-lt" aria-label="Permalink to &quot;水平触发（LT）&quot;">​</a></h3> <ol><li>只要 socket 缓冲区还有数据，epoll_wait 就会一直触发，直到缓冲区读空；</li> <li>LT 是 epoll 的默认工作模式；</li> <li>优缺点：优点是保证数据完全读取；缺点是数据量大时用户态/内核态切换频繁，消耗资源；</li> <li>适用：连接请求少、客户端单次数据量小的场景——一次性收完所有数据，此时若用边缘触发反而多一次「读到 EAGAIN」的判断调用。</li></ol> <h3 id="边缘触发-et" tabindex="-1">边缘触发（ET） <a class="header-anchor" href="#边缘触发-et" aria-label="Permalink to &quot;边缘触发（ET）&quot;">​</a></h3> <ol><li>只有套接字事件状态改变（新事件到来）时才触发；</li> <li>ET 必须配合非阻塞 IO：每次 epoll_wait 触发后，在 while 循环里非阻塞地读到缓冲区为空；</li> <li>优缺点：优点是一次触发就能读完缓冲区所有数据，效率高；缺点是数据量很小时至少要调用两次 IO 函数（最后一次读到 EAGAIN），而 LT 只需一次。</li></ol> <h3 id="怎么选" tabindex="-1">怎么选 <a class="header-anchor" href="#怎么选" aria-label="Permalink to &quot;怎么选&quot;">​</a></h3> <p>一种常见的取舍是：<strong>任何情况都优先选「ET + 非阻塞 IO」</strong>。理由：连接多、数据量大时，ET 一次触发即可读空缓冲区，效率优势明显；连接少、数据量小时，服务本身性能压力不大，ET 多出的那次「读到 EAGAIN」开销可以忽略——同一个选择同时覆盖两种负载。</p> <h3 id="lt-也需要非阻塞-io" tabindex="-1">LT 也需要非阻塞 IO <a class="header-anchor" href="#lt-也需要非阻塞-io" aria-label="Permalink to &quot;LT 也需要非阻塞 IO&quot;">​</a></h3> <p>无论 LT 还是 ET，都应该用<strong>非阻塞 IO</strong>。反证：假设 LT 用阻塞 read 且要求一次读满 20 字节，客户端只发了 10 字节，线程就会阻塞在 read 里等剩余 10 字节；但线程阻塞后无法回到 epoll_wait 监听该连接的下一次就绪——客户端后续数据再也无法接收，read 永远凑不够 20 字节，逻辑上死锁。</p> <p>ET 的道理相同且更直接：ET 是无限循环读直到 EAGAIN；阻塞 IO 在缓冲区读空时会把整个线程阻塞在 read 上，其他监听事件全部无法处理。</p> <h2 id="epolloneshot" tabindex="-1">EPOLLONESHOT <a class="header-anchor" href="#epolloneshot" aria-label="Permalink to &quot;EPOLLONESHOT&quot;">​</a></h2> <p>即使使用 ET 模式，一个 socket 上的事件仍可能被触发多次：一个线程读某 socket 的数据并处理期间，socket 上又有新数据可读（EPOLLIN 再次触发），另一个线程被唤醒来读——两个线程同时操作一个 socket。</p> <p>「一个连接在任意时刻只被一个线程处理」可以用 EPOLLONESHOT 实现：注册了 EPOLLONESHOT 的 fd，操作系统最多触发其注册的一个可读、可写或异常事件，<strong>且只触发一次</strong>，除非用 epoll_ctl <strong>重置</strong>。这样处理期间其他线程没有机会碰这个 socket。注意配套动作：<strong>该线程处理完后应立即重置 EPOLLONESHOT，否则这个 socket 的后续事件再也不会被触发</strong>。</p> <h2 id="要点" tabindex="-1">要点 <a class="header-anchor" href="#要点" aria-label="Permalink to &quot;要点&quot;">​</a></h2> <ul><li>单 Reactor 多线程：主线程 epoll_wait + 事件分发，线程池做业务；主从 Reactor 用「MainReactor 只管 accept、SubReactor 管已有连接」简化交互</li> <li>Reactor 感知「就绪」、应用自己读；Proactor 感知「完成」、内核代读——Linux 上多为模拟实现</li> <li>epoll 的两个关键设计：内核红黑树长期持有 fd 集合（免重复拷贝）+ 就绪链表回调（免全量遍历）</li> <li>ET/LT 都要配非阻塞 IO；负载不确定时「ET + 非阻塞」是通用选择</li> <li>多线程操作同一连接用 EPOLLONESHOT 串行化，处理完记得重置</li></ul>',77))])}const L=E(A,[["render",B]]);export{f as __pageData,L as default};
